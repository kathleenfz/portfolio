{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/articles", "dest": "/" },
    { "src": "/copywriting", "dest": "/" },
    { "src": "/(.*)", "dest": "/" }
  ]
}